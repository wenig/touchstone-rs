# Baseline

A random-score detector that returns a uniform `[0, 1)` sample for every incoming point, regardless of its value or dimension. It exists as a sanity-check floor: any real detector should outperform it on a non-trivial benchmark.
